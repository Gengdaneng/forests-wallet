The container for everything. `tone="ink"` is reserved for the balance hero; `honey` for goal/pocket-money content; `leaf` for rules and income.

```jsx
<Card variant="child" tone="honey"><h3>攒钱目标</h3></Card>
```
