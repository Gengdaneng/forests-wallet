One Lucide line icon at the brand's 2px stroke weight; use it anywhere an icon appears, and always next to text.

```jsx
<Icon name="piggy-bank" size={28} color="var(--money-goal)" />
```

Requires the Lucide UMD script on the page. Decorative by default; pass `label` only when the icon carries meaning no adjacent text carries.
