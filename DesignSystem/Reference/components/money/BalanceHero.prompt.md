Top third of both home screens. Pass cents; it handles the negative-balance language ("你现在欠爸爸 20") itself.

```jsx
<BalanceHero cents={8700} size="child" />
<BalanceHero cents={-2000} size="child" />
```
