A ledger line in the timeline. Records are never removed — a corrected record stays with `reversed`.

```jsx
<TransactionRow reason="买冰淇淋" cents={1500} direction="out" category="food" date="10月3日" balanceAfter={7200} />
```
