This week's check-in grid, with the legend that makes the three states legible.

```jsx
<WeekBoard weekLabel="本周" items={[{name:'跳绳',goal:5,rewardCents:500,days:['done','done','unlogged','future','future','future','future']}]} />
```

Pass `onToggle` only on Dad's iPhone. Never render a cross for an unlogged day.
