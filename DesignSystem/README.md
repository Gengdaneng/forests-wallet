# Design system reference

Visual and interaction authority for Forrest's Wallet.

## Provenance

- Source filename: `Forrest's Wallet Design System.zip`
- SHA-256: `9108304b65f9b62ec21e3fd566d351ced1f9f1dce3862ab9397867744a1f125d`

The complete extracted source is in `Reference/`. Bytes are preserved from the archive. The ZIP itself is not committed.

This tree is **design input**, not an executable tool and not an app dependency. Do not run bundled JavaScript (`_ds_bundle.js`, `support.js`, template scripts) or install it as a web package.

The archive did not include license terms; none are stated or invented here.

Native SwiftUI tokens and components live in `ios/` and translate this system rather than wrapping it.
