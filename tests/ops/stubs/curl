#!/bin/sh
# Test double for curl. Records a ping without printing the URL.
set -eu

if [ -n "${FW_CURL_LOG:-}" ]; then
  echo "called" >>"$FW_CURL_LOG"
fi
if [ "${FW_CURL_FAIL:-0}" = "1" ]; then
  exit 1
fi
exit 0
